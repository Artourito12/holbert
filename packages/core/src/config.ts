/**
 * Identité de la plateforme — le nom commercial peut changer :
 * il ne doit JAMAIS être écrit en dur ailleurs que dans ce fichier.
 */
export const PLATFORM_NAME = "Holbert";

/** Nom commercial de la suite complète (les trois modules ensemble). */
export const SUITE_NAME = "Orders";
